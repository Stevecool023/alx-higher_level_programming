#!/usr/bin/node
/*
 a script that prints a multi-lines using an array of string and a loop.
 */

const myLines = ['C is fun', 'Python is cool', 'JavaScript'];
let i = 0;
for (i; i < myLines.length; i++) {
  console.log(myLines[i]);
}
